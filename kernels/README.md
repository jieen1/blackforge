# Kernels

Add SM120-specific CUDA/C++ sources by operation (`attention/`, `gdn/`,
`nvfp4_gemm/`, `fused_ops/`, and `bindings/`). Every replacement needs oracle
fixtures and a microbenchmark before it reaches the serving path.
